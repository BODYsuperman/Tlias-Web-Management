<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.mapper.ClassMapper">

    <insert id="insertClass">
        INSERT INTO clazz (name,room,begin_date,end_date,master_id,subject,create_time,update_time) VALUES
        (#{name},#{room},#{beginDate},#{endDate},#{masterId},#{subject},#{createTime},#{updateTime})
    </insert>

    <update id="update">
        UPDATE clazz SET name = #{name},room = #{room},
        begin_date = #{beginDate},end_date = #{endDate},
        master_id = #{masterId},subject = #{subject},update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM clazz WHERE id = #{id};
    </delete>

    <select id = "list" resultType = "com.alex.pojo.Clazz">
        SELECT c.*,e.name AS masterName FROM
        clazz c LEFT OUTER JOIN emp e
        ON c.master_id = e.id
        <where>
            <if test = "name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test = "begin != null and end != null">
                AND c.end_date BETWEEN #{begin} AND #{end}
            </if>
        </where>
    </select>

    <select id="countStudent" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM student GROUP BY clazz_id HAVING clazz_id = #{id};
    </select>


</mapper>